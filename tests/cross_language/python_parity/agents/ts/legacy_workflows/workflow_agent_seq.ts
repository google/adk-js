/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */

/**
 * TS counterpart of adk-python
 * contributing/samples/legacy_workflows/workflow_agent_seq.
 *
 * Writer -> Reviewer -> Refactorer, chained through session state: each stage
 * writes `outputKey` (Python's `output_key`) and the next reads it back with a
 * `{key}` placeholder in its instruction. The instructions embed triple
 * backticks, escaped here because TS uses backtick template literals.
 */
import {LlmAgent, SequentialAgent} from '@google/adk';

import {PARITY_MODEL} from '../model.ts';

// Part of agent.py --> Follow https://google.github.io/adk-docs/get-started/quickstart/ to learn the setup

// --- 1. Define Sub-Agents for Each Pipeline Stage ---

// Code Writer Agent
// Takes the initial specification (from user query) and writes code.
const codeWriterAgent = new LlmAgent({
  name: 'CodeWriterAgent',
  model: PARITY_MODEL,
  // Change 3: Improved instruction
  instruction: `You are a Python Code Generator.
Based *only* on the user's request, write Python code that fulfills the requirement.
Output *only* the complete Python code block, enclosed in triple backticks (\`\`\`python ... \`\`\`).
Do not add any other text before or after the code block.
`,
  description: 'Writes initial Python code based on a specification.',
  outputKey: 'generated_code', // Stores output in state['generated_code']
});

// Code Reviewer Agent
// Takes the code generated by the previous agent (read from state) and provides feedback.
const codeReviewerAgent = new LlmAgent({
  name: 'CodeReviewerAgent',
  model: PARITY_MODEL,
  // Change 3: Improved instruction, correctly using state key injection
  instruction: `You are an expert Python Code Reviewer.
    Your task is to provide constructive feedback on the provided code.

    **Code to Review:**
    \`\`\`python
    {generated_code}
    \`\`\`

**Review Criteria:**
1.  **Correctness:** Does the code work as intended? Are there logic errors?
2.  **Readability:** Is the code clear and easy to understand? Follows PEP 8 style guidelines?
3.  **Efficiency:** Is the code reasonably efficient? Any obvious performance bottlenecks?
4.  **Edge Cases:** Does the code handle potential edge cases or invalid inputs gracefully?
5.  **Best Practices:** Does the code follow common Python best practices?

**Output:**
Provide your feedback as a concise, bulleted list. Focus on the most important points for improvement.
If the code is excellent and requires no changes, simply state: "No major issues found."
Output *only* the review comments or the "No major issues" statement.
`,
  description: 'Reviews code and provides feedback.',
  outputKey: 'review_comments', // Stores output in state['review_comments']
});

// Code Refactorer Agent
// Takes the original code and the review comments (read from state) and refactors the code.
const codeRefactorerAgent = new LlmAgent({
  name: 'CodeRefactorerAgent',
  model: PARITY_MODEL,
  // Change 3: Improved instruction, correctly using state key injection
  instruction: `You are a Python Code Refactoring AI.
Your goal is to improve the given Python code based on the provided review comments.

  **Original Code:**
  \`\`\`python
  {generated_code}
  \`\`\`

  **Review Comments:**
  {review_comments}

**Task:**
Carefully apply the suggestions from the review comments to refactor the original code.
If the review comments state "No major issues found," return the original code unchanged.
Ensure the final code is complete, functional, and includes necessary imports and docstrings.

**Output:**
Output *only* the final, refactored Python code block, enclosed in triple backticks (\`\`\`python ... \`\`\`).
Do not add any other text before or after the code block.
`,
  description: 'Refactors code based on review comments.',
  outputKey: 'refactored_code', // Stores output in state['refactored_code']
});

// --- 2. Create the SequentialAgent ---
// This agent orchestrates the pipeline by running the sub_agents in order.
const codePipelineAgent = new SequentialAgent({
  name: 'CodePipelineAgent',
  subAgents: [codeWriterAgent, codeReviewerAgent, codeRefactorerAgent],
  description:
    'Executes a sequence of code writing, reviewing, and refactoring.',
  // The agents will run in the order provided: Writer -> Reviewer -> Refactorer
});

// For ADK tools compatibility, the root agent must be named `root_agent`
export const rootAgent = codePipelineAgent;
